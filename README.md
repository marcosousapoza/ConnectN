# PrinciplesOfAI-connect-n

# How to run
You can run the python code by typing `python -m <path of connect_n folder>`
To play with different settings/different AIs one must change the App.launch function.